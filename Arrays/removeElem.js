const numbers = [1,2,3,4]

//end:
const last = numbers.pop()

//middle:
//pass the index and the number of the elements you want to delete
numbers.splice(2,1)

//beginning:
const fist = numbers.shift()